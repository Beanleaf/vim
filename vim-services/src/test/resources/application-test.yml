spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
  main:
    banner-mode: off
  mail:
    host:
    port:
    username:
    password:
logging:
  level:
    ROOT: INFO
    be: DEBUG